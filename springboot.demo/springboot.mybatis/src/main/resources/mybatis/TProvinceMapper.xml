<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hsy.springboot.demo.springboot.mybatis.mapper.TProvinceMapper">
    <!--<resultMap id="defaultProvinceMap" type="TProvince" >
        <id property="id" column="id"></id>
        <result property="code" column="code"></result>
        <result property="name" column="name"></result>
        <result property="sortId" column="sort_id"></result>
    </resultMap>

    <select id="getProvinceList" resultMap="defaultProvinceResultMap">
        select * from t_province
    </select>

    <select id="getProvinceById" resultMap="defaultProvinceResultMap">
        select * from t_province WHERE 1 = 1
        <if test="id!=null || ''!=id">
            AND id = #{id}
        </if>
    </select>-->
</mapper>